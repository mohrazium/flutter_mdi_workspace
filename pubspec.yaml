name: flutter_mdi_workspace
description: A reusable Flutter MDI (Multiple Document Interface) workspace package for desktop and web applications. Provides draggable, resizable, and manageable internal windows inspired by desktop GUI behavior.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
